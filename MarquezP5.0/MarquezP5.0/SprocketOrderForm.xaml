<Window x:Class="MarquezP5._0.SprocketOrderForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MarquezP5._0"
        Title="Marquez - Spacely Sprockets Orders Application" Height="520" Width="720" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Label Content="Customer Name:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox x:Name="txtCustomerName" Width="260" Margin="10,0,20,0" VerticalAlignment="Center"/>
            <CheckBox x:Name="chkLocalPickup" Content="Local Pickup" Checked="ChkLocalPickup_Toggle" Unchecked="ChkLocalPickup_Toggle" VerticalAlignment="Center"/>
        </StackPanel>

        <GroupBox x:Name="grpAddress" Header="Shipping Destination Address" Grid.Row="1" Margin="0,0,0,12">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="65"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>

                <Label Content="Street Address:" Grid.Row="0" Grid.Column="0" Margin="3"/>
                <TextBox x:Name="txtStreet" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="5" Margin="3"/>

                <Label Content="City:" Grid.Row="1" Grid.Column="0" Margin="3"/>
                <TextBox x:Name="txtCity" Grid.Row="1" Grid.Column="1" Margin="3"/>

                <Label Content="State:" Grid.Row="1" Grid.Column="2" Margin="3"/>
                <TextBox x:Name="txtState" Grid.Row="1" Grid.Column="3" Margin="3"/>

                <Label Content="Zip Code:" Grid.Row="1" Grid.Column="4" Margin="3"/>
                <TextBox x:Name="txtZip" Grid.Row="1" Grid.Column="5" Margin="3"/>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="2" Margin="0,5,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ListBox x:Name="lstSprockets" Margin="0,0,15,0"/>

            <StackPanel Grid.Column="1" Width="130">
                <Button Content="Add Sprocket" Margin="0,0,0,12" Padding="6" Click="BtnAdd_Click"/>
                <Button Content="Remove Selected" Margin="0,0,0,12" Padding="6" Click="BtnRemove_Click"/>
                <Button Content="Update &amp; Save" Padding="6" Click="BtnSave_Click"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Label Content="Total Balance Due:" FontSize="14" FontWeight="Bold"/>
            <TextBlock x:Name="lblTotalPrice" Text="$0.00" FontSize="14" FontWeight="Bold" Margin="5,5,10,5" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>